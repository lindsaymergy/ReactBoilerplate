var Components = require('expose-loader?Components!./components');

// TODO: Change to "server components" that would extend base classes 
// holding only server properties, in order to render anything but js files. 
// e.g css, html, images, etc.